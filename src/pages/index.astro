---
import Main from "../layouts/Main.astro";
import { Image } from "astro:assets";
import geggobanner from "../assets/geggobanner.png";
import LetterboxdWidget from "../components/LetterboxdWidget.astro";
import DiscordWidget from "../components/DiscordWidget.astro";
import TimeWidget from "../components/TimeWidget.astro";

---
<Main>
    <div class="image-container flex flex-col items-center justify-end w-full relative left-1/2 -translate-x-1/2 overflow-hidden">
        <Image
            src={geggobanner}
            alt="a banner of Geggos and his friends"
            class="index-banner"
            width={2048}
            height={940}
        />
    </div>
    <div class="my-8 text-center flex flex-col px-6">
        <h1>Welcome to Geggos.net!</h1>
        <hr />
        <div class="mt-8 index-content grid grid-cols-1 md:grid-cols-2 gap-8 w-full">
            <div class="left-column flex flex-col gap-8">
                <div class="self-center md:self-end">
                    <DiscordWidget />
                </div>
                <div class="self-center md:self-end">
                    <LetterboxdWidget />
                </div>
            </div>
            <div class="flex flex-col right-column md:row-span-2">
                <div class="self-center md:self-start">
                    <TimeWidget />
                </div>
                <div class="self-center md:self-start">
                   POBII you are not safe. Wake up. Wake up. Wake up. Wake up.
                </div>
            </div>
        </div>
    </div>
</Main>

<style>
    .index-banner {
        object-fit: cover;
        object-position: center top;
        width: 100%;
        height: 100%;
        pointer-events: none;
        user-select: none;
        max-width: 1300px;
    }

    .image-container {
        pointer-events: none;
        user-select: none;
        height: 400px;
        width: 100vw;
        background-color: rgb(255, 245, 255);
        border-bottom: 4px solid var(--bg-accent-transparent);
    }

    @media (max-width: 768px) {
        .image-container {
            height: 250px;
        }
        .index-banner {
            height: 95%;
        }
    }

    @media (hover:hover){
        h1:hover {
            transform: rotate(-0.5deg) !important;
        }
        h1:active {
            transform: scale(0.95) !important;
        }
    }

</style>
